// Welcome to my journey as a developer, although it seems impossible for now. I will definitely pursue this career.

// This is an example of simple object

const colors = {
  primary: 'blue',
  secondary: 'green',
  tertiary: 'red'
};

// Accesing the Object
console.log(colors);

// Accesing the values of the Object
console.log(colors.primary);
console.log(colors.secondary);
console.log(colors.tertiary);

// Putting an expression inside 
console.log(`These are the colors of this object ${colors.primary}, ${colors.secondary}, ${colors.tertiary}.`);






